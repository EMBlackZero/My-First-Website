function StudentPage() {
  const containerStyle = {
    backgroundColor: "#f0f0f0",
    padding: "20px",
    borderRadius: "8px",
    boxShadow: "0 0 10px rgba(0, 0, 0, 0.1)",
    textAlign: "center",
    fontSize: "1.5em",
    fontWeight: "bold",
  };

  return <div style={containerStyle}>Student Page</div>;
}

export default StudentPage;
